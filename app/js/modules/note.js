export default class Note {
    constructor(notes, wrapper) {
        this.notes = document.querySelectorAll(notes);
        this.wrapper = document.querySelector(wrapper);
    }

    editNote() {
        
    }
}